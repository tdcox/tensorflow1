# Default values for python.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: 10.59.251.141:5000/tdcox/tensorflow1
  tag: 0.0.9
  pullPolicy: IfNotPresent
service:
  name: tensorflow1
  type: ClusterIP
  externalPort: 80
  internalPort: 8080
  annotations:
    fabric8.io/expose: "true"
    fabric8.io/ingress.annotations: "kubernetes.io/ingress.class: nginx"
resources:
  limits:
    cpu: 2000m
    memory: 3Gi
  requests:
    cpu: 2000m
    memory: 3Gi
ingress:
  enabled: false
